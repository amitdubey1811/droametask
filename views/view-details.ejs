<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p> Customers Details are given below:</p>
    <div style="border:2px seagreen; margin: 50px;">
    <div class="quotes" >
        <!-- Loop through quotes -->
        <% for(var i = 0; i < details.length; i++) {%>
          <div class="quote" style="border-radius: 1px red ;">
            <!-- Output name from the iterated quote object -->
            <div> Customer_id:<%= details[i]._id%></div>
            <div>Customer Name:<%= details[i].fullname %></div>
            <!-- Output quote from the iterated quote object -->
            <div>ID_no:<%= details[i].customer_id %></div>
            <div>Phone :<%= details[i].phone %></div>
            <div> Customer email:<%= details[i].email %></div>
           

          </div>
          <br>
        <% } %>
        </div>
    </div>
</body>
</html>
